# ============================================================
# Cloudflare Named Tunnel — Config Template
# Generated by: deployment/setup_named_tunnel.ps1
# DO NOT edit manually — run the setup script instead.
# ============================================================

tunnel: TUNNEL_ID_PLACEHOLDER
credentials-file: CREDENTIALS_FILE_PLACEHOLDER

ingress:
  # Route all traffic to the local FastAPI server
  - service: http://localhost:8080

# Logging
loglevel: info
